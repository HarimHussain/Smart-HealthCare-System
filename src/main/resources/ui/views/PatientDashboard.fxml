<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane prefHeight="471.0" prefWidth="586.0" styleClass="dashboard-container full-height full-width" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="ui.controllers.PatientDashboardController">

    <!-- Left Sidebar -->
    <left>
        <VBox spacing="0" styleClass="sidebar">
            <!-- Sidebar Header -->
            <VBox spacing="4" styleClass="sidebar-header">
                <Label styleClass="sidebar-title" text="🏥 Smart Healthcare" />
                <Label fx:id="userNameLabel" styleClass="sidebar-user" text="Harim" />
                <Label fx:id="userEmailLabel" styleClass="sidebar-user" text="Patient" />
            </VBox>

            <!-- Navigation Menu -->
            <ScrollPane fitToWidth="true" hbarPolicy="NEVER" style="-fx-background: transparent; -fx-background-color: transparent;" vbarPolicy="AS_NEEDED">
                <VBox spacing="8" style="-fx-padding: 20;">
                    <Button onAction="#handleDashboard" styleClass="nav-item" text="📊 Dashboard">
                        <graphic>
                            <Text />
                        </graphic>
                    </Button>
                    <Button onAction="#handleBookAppointment" styleClass="nav-item" text="📅 Book Appointment">
                        <graphic>
                            <Text />
                        </graphic>
                    </Button>
                    <Button onAction="#handleViewAppointments" styleClass="nav-item" text="📋 My Appointments">
                        <graphic>
                            <Text />
                        </graphic>
                    </Button>
                    <Button onAction="#handleMedicalHistory" styleClass="nav-item" text="🏥 Medical History">
                        <graphic>
                            <Text />
                        </graphic>
                    </Button>
                    <Button onAction="#handleProfile" styleClass="nav-item" text="👤 Profile Settings">
                        <graphic>
                            <Text />
                        </graphic>
                    </Button>

                    <VBox VBox.vgrow="ALWAYS" />

                    <Button onAction="#handleLogout" styleClass="nav-item" text="🚪 Logout">
                        <graphic>
                            <Text />
                        </graphic>
                    </Button>
                </VBox>
            </ScrollPane>
        </VBox>
    </left>

    <!-- Main Content Area -->
    <center>
        <ScrollPane fitToWidth="true" hbarPolicy="NEVER" style="-fx-background: transparent; -fx-background-color: transparent;" vbarPolicy="AS_NEEDED">
            <VBox styleClass="content-area">
                <!-- Header -->
                <HBox alignment="CENTER_LEFT" spacing="16" styleClass="header">
                    <VBox>
                        <Label fx:id="dashboardTitle" styleClass="header-title" text="Patient Dashboard" />
                        <Label fx:id="dashboardSubtitle" styleClass="header-subtitle" text="Welcome back to your healthcare portal" />
                    </VBox>
                    <HBox HBox.hgrow="ALWAYS" />
                    <Button onAction="#handleBookAppointment" style="-fx-font-size: 14px; -fx-padding: 10px 20px;" styleClass="btn-primary" text="+ New Appointment" />
                </HBox>

                <!-- Content Pane -->
                <StackPane fx:id="contentPane" style="-fx-min-height: 600px;" />
            </VBox>
        </ScrollPane>
    </center>
</BorderPane>
